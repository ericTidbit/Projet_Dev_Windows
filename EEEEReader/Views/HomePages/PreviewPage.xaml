<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="EEEEReader.Views.PreviewPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:EEEEReader.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <ScrollViewer
        HorizontalAlignment="Left"
        VerticalAlignment="Top"
        HorizontalScrollBarVisibility="Disabled"
        HorizontalScrollMode="Disabled"
        VerticalScrollBarVisibility="Auto"
        VerticalScrollMode="Enabled"
        ZoomMode="Disabled">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Image
                x:Name="PreviewImage"
                Grid.Column="0"
                Width="300"
                Height="400"
                Margin="10"
                Source="{x:Bind PreviewedLivre.CoverImage}" />
            <StackPanel
                Grid.Column="1"
                Margin="15"
                Orientation="Vertical">
                <TextBlock
                    FontSize="24"
                    FontWeight="Bold"
                    Text="{x:Bind PreviewedLivre.Titre}" />
                <TextBlock
                    FontSize="18"
                    Foreground="Gray"
                    Text="{x:Bind PreviewedLivre.Auteur}" />
                <Button
                    Width="150"
                    Margin="0,10,0,10"
                    Click="LireLivre_Click"
                    Content="Lire le livre" />
                <TextBlock
                    Width="Auto"
                    FontSize="14"
                    Text="{x:Bind PreviewedLivre.Resume}"
                    TextWrapping="Wrap" />
            </StackPanel>
        </Grid>
    </ScrollViewer>
</Page>
