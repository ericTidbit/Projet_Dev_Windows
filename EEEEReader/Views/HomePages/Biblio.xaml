<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="EEEEReader.Views.Biblio"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:EEEEReader.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:model="using:EEEEReader.Models"
    mc:Ignorable="d">

    <Grid Padding="10" RowSpacing="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="AUTO" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0">
            <Button Click="SelectionFichier" Content="Choisir un fichier" />
        </StackPanel>
        <GridView x:Name="MyListView" Grid.Row="1">
            <GridView.ItemTemplate>
                <DataTemplate x:DataType="model:Livre">
                    <StackPanel
                        Padding="10"
                        Orientation="Vertical"
                        Spacing="10">
                        <Image
                            Width="100"
                            Height="150"
                            Source="/Assets/Square150x150Logo.png" />
                        <!--  Source="{x:Bind CoverImage}" />  -->
                        <TextBlock FontWeight="Bold" Text="{x:Bind Titre}" />
                        <TextBlock Foreground="Gray" Text="{x:Bind Auteur}" />
                    </StackPanel>
                </DataTemplate>
            </GridView.ItemTemplate>
        </GridView>
    </Grid>
</Page>
