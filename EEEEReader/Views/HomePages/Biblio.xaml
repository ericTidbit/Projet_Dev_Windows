<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="EEEEReader.Views.Biblio"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:EEEEReader.Views"
    xmlns:model="using:EEEEReader.Models"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="AUTO"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0">
            <Button Content="Choisir un fichier" Click="SelectionFichier"/>
        </StackPanel>
        <ListView x:Name="MyListView" Grid.Row="1">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="model:Livre">
                    <StackPanel Orientation="Horizontal" Spacing="10">
                        <TextBlock Text="{x:Bind Titre}" FontWeight="Bold"/>
                        <TextBlock Text="{x:Bind Auteur}" Foreground="Gray"/>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
