<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="EEEEReader.Views.ReadingPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:EEEEReader.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:converter="using:EEEEReader.Converter"
    mc:Ignorable="d">

    <Page.Resources>
        <converter:HtmlDocumentToRichTextBlockConverter x:Key="HtmlDocumentToRichTextBlockConverter"/>
    </Page.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>    
    <ScrollViewer
        Grid.Row="0"
        HorizontalAlignment="Stretch"
        VerticalAlignment="Top"
        HorizontalScrollBarVisibility="Disabled"
        HorizontalScrollMode="Disabled"
        VerticalScrollBarVisibility="Auto"
        VerticalScrollMode="Enabled"
        ZoomMode="Disabled">
            <ContentControl 
                Content="{x:Bind ReadingViewModel.CurrentHtml,Mode=OneWay, Converter={StaticResource HtmlDocumentToRichTextBlockConverter}}"
                HorizontalAlignment="Stretch"
                Padding="30 5 30 0"/>
        </ScrollViewer>
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0" Margin="10" VerticalAlignment="Center" Text="{x:Bind ReadingViewModel.FooterText, Mode=OneWay}"/>
            <Button Grid.Column="1" Height="50" Width="50" Margin="10 5" Click="ButtonPrev_OnClick">
                <FontIcon Glyph="&#xE72B;"/>
            </Button>
            <Button Grid.Column="2" Height="50" Width="50" Margin="10 5" Click="ButtonBack_OnClick">
                <FontIcon Glyph="&#xE751;"/>
            </Button>
            <Button Grid.Column="3" Height="50" Width="50" Margin="10 5" Click="ButtonNext_OnClick">
                <FontIcon Glyph="&#xE72A;"/>
            </Button>
        </Grid>
    </Grid>
</Page>
